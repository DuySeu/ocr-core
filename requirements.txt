# OCR engine
pytesseract
paddleocr
easyocr

# Input loading: PDF -> page images (pypdfium2, no Poppler binary needed)
pypdfium2
Pillow

# Preprocessing (orientation, deskew, denoise), table grid detection (OpenCV)
numpy
opencv-python

# Config file parsing (YAML)
PyYAML

# Evaluation harness (evaluate/): WER/CER, TEDS, IoU
rapidfuzz
apted
python-docx
lxml  # table markup parsing; also used by core/document/validate.py
